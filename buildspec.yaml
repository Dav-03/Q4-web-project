version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
      - echo Initializing environment
      - export CODEARTIFACT_AUTH_TOKEN=`aws codeartifact get-authorization-token --domain david-college-projects --domain-owner 528757827860 --region us-west-2 --query authorizationToken --output text`
<<<<<<< HEAD:buildspec.yml
=======

>>>>>>> 0461eda9abe7a00c45cdd5d9c27bc07fce6ca0d3:buildspec.yaml
  build:
    commands:
      - echo Build started on `date`
      - mvn -s settings.xml compile
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn -s settings.xml package
artifacts:
  files:
    - target/nextwork-web-project.war
    - appspec.yml
    - scripts/**/*
  discard-paths: no

